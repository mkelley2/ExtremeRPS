<!DOCTYPE html>
<html>
    <head>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <title>Rock Paper Scissors Game</title>
    </head>
    <body>
        <div class="container">
            <h1>Rock Paper Scissors Game</h1>
            <div class="form-group">
                <form action="/" method="post">
                    <label for="player1Input">Player 1: Enter R, P, or S:</label>
                    <input type="password" name="player1Input" class="form-control" maxlength="1">
                    <br>
                    <label for="player2Input">Player 2: Enter R, P, or S:</label>
                    <input type="password" name="player2Input" class="form-control" maxlength="1">

                    <button type="submit" class="btn btn-info">Submit</button>
                </form>
            </div>

            <div class="result">
                {% if all_games is not empty %}
                    {% for game in all_games %}
                        <h2>{{ game }}</h2>
                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </body>
</html>
